# PRD (v0) reference

- /implement creates GitHub Issue (if missing) + PR linked via `Closes #N`
- All subagents model=opus
- macOS/Linux only
- UX scope: docs/ux_spec.md only (no UI code generation)
- CI validates structure, lints scripts, runs unit tests
